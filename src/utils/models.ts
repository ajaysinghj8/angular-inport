export type ElementBoundingPositions = {
  top: number;
  right: number;
  bottom: number;
  left: number;
};

export type WindowElement = HTMLElement | Window;

export type Point = {
  x: number;
  y: number;
}
